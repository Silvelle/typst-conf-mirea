// Содержание — a generated outline, three levels deep, exactly the
// `TOC \h \z \t "1 Заголовок;1;2 Заголовок;2;3 Заголовок;3"` field that
// Реферат_2_2.docx carries. Nothing to maintain by hand: adding a heading
// anywhere in content/ updates this page on the next compile.
#import "../template/report.typ": contents

#contents()
